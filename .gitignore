.venv/
__pycache__/
*.pyc
.pytest_cache/
node_modules/
soyoil/data/cache/
# web/dist and soyoil/models artifacts are committed on purpose so cloud
# hosts can serve the app with a plain `pip install` — no Node, no training.
nohup.out
*.log
web/tsconfig.tsbuildinfo

# PIMS: the demo database is generated on first run (`python -m pims init-db`).
data/pims.db
data/pims.db-wal
data/pims.db-shm
pimsweb/tsconfig.tsbuildinfo
# The sandbox bundle is a build artifact of `npm run build:demo`.
pimsweb/dist-demo/
